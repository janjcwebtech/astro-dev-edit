/** A string constant living in another module — the `elsewhere` verdict's one
 *  fixture. A prop reading this is writable, just not in the file passing it. */
export const TAGLINE = 'Written in another module';
